/**
 * Integrated template adapter for Jumplate
 */
var TemplateAdapter = {
	compile: async function compile(templatePath, layoutPath) {

	},

	render: async function render(compiledTemplate, data) {

	},

	compileAndRender: async function compileAndRender(templatePath, layoutPath, data) {

	},
};

module.exports = TemplateAdapter;